#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main(int argc, char *argv[]) {
	// write c program to concantenate two strings
	//char d[20]="Ahmet",d2[20]="Ali";
/*	strcat(d,d2);
	printf("%s",d);
	*/
	//printf("%s%s",d,d2);
	
	// write c program to remove all characters in a string except alphabet
	/*char d[20];
	int i=0;
	printf("enter text:");
	scanf("%s",&d);
	while(d[i]){
		if(d[i]>='A' && d[i]<='Z' || d[i]>='a' && d[i]<='z'){
			printf("%c",d[i]);
		}
		i++;
	}*/
	// write c program to count the number of vowels consonants and so on
	/*char d[50]="adslkj34 34lkj343 34lk";
	int v=0,c=0,n=0,w=0,i=0,o=0;
	strupr(d);
	while(d[i]){
		if(d[i]>='A' && d[i]<='Z'){
			if(d[i]== 'A' || d[i]=='E' || d[i]=='I' || d[i]=='O' || d[i]=='U'){
				v++;
			}
			else{
				c++;
			}
		}
		else if(d[i]>='0' && d[i]<'9'){
			n++;
		}
		else if(d[i]==' '){
			w++;
		}
		else
		{
			o++;
		}
		i++;
		
	}
	printf("the number of vowels word is:%d\n",v);
	printf("the number of constant word is:%d\n",c);
	printf("the number ofnumber is:%d\n",n);
	printf("the number of white space is%d\n",w);
	printf("the number of others things:%d\n",o);
	*/
	//Write C Program to Sort Strings in Dictionary Order
return 0;
}
